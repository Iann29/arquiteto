# Inicia com cd src && python main.py

# 💡 A Ideia do Sistema de Nodes

## O que é?

O sistema de nodes é uma **configuração visual** do que vai acontecer quando você rodar um projeto.

Em vez de lembrar de cabeça "ah, pra trabalhar no Use Gaba eu preciso abrir o Zed, subir o Docker, abrir o terminal...", você monta isso visualmente uma vez e depois é só executar.

**Exemplo simples:**
```
[Projeto Iniciado: Use Gaba] → [Abrir] → [Zed]
```

Isso significa: "Quando eu executar esse workflow, vai abrir o Zed no projeto Use Gaba".

É tipo um mapa/blueprint visual que você configura do jeito que quiser, e depois o sistema executa tudo automaticamente pra você.

---
